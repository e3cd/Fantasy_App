<%= form_with(model: roster_spot, local: true) do |form| %>
  <% if roster_spot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(roster_spot.errors.count, "error") %> prohibited this roster_spot from being saved:</h2>

      <ul>
      <% roster_spot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
